apiVersion: v1
kind: Service
metadata:
  name: jobber-chat
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: jobber-chat
  ports:
  - name: chat
    port: 4007
    targetPort: 4007
    protocol: TCP